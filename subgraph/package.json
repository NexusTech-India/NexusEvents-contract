{
  "name": "nexus-events",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ nexus-events",
    "create-local": "graph create --node http://localhost:8020/ nexus-events",
    "remove-local": "graph remove --node http://localhost:8020/ nexus-events",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 nexus-events",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.63.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
